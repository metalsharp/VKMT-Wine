diff --git a/Docs/MoltenVK_Configuration_Parameters.md b/Docs/MoltenVK_Configuration_Parameters.md
index fdb7b9fa..43daff7f 100644
--- a/Docs/MoltenVK_Configuration_Parameters.md
+++ b/Docs/MoltenVK_Configuration_Parameters.md
@@ -658,3 +658,19 @@ In the special case of `VK_SEMAPHORE_TYPE_TIMELINE` semaphores, **MoltenVK** wil
 Makes MoltenVK treat all descriptors as if they had `VK_DESCRIPTOR_BINDING_PARTIALLY_BOUND_BIT` set.
 Versions of MoltenVK with this flag are less forgiving of applications that bind descriptors that point to
 destroyed objects, so this option can be used to temporarily work around any breakage that may have caused.
+
+----------------------------------------
+#### MVK_CONFIG_ADVERTISE_ROBUST_BUFFER_ACCESS_2
+
+##### Type: Boolean
+##### Default: `1`
+
+Advertise the `VkPhysicalDeviceRobustness2FeaturesEXT::robustBufferAccess2` feature
+(on Apple family GPUs only).
+
+Metal does not bounds-check buffer accesses in shaders, and MoltenVK does not inject
+MSL bounds checks, so out-of-bounds behaviour is not strictly guaranteed by MoltenVK
+itself. On Apple family GPUs, out-of-bounds buffer accesses are contained by the GPU
+memory subsystem in practice, and D3D12 translation layers such as vkd3d-proton apply
+their own shader-level bounds behaviour. Disable this setting if strict Vulkan
+robustness guarantees are required.
diff --git a/MoltenVK/MoltenVK/API/mvk_private_api.h b/MoltenVK/MoltenVK/API/mvk_private_api.h
index 11155a4b..eeed937c 100644
--- a/MoltenVK/MoltenVK/API/mvk_private_api.h
+++ b/MoltenVK/MoltenVK/API/mvk_private_api.h
@@ -247,6 +247,7 @@ typedef struct {
 	const char* shaderDumpDir;                                                 /**< MVK_CONFIG_SHADER_DUMP_DIR */
 	VkBool32 shaderLogEstimatedGLSL;                                           /**< MVK_CONFIG_SHADER_LOG_ESTIMATED_GLSL */
 	VkBool32 liveCheckAllResources;                                            /**< MVK_CONFIG_LIVE_CHECK_ALL_RESOURCES */
+	VkBool32 advertiseRobustBufferAccess2;                                     /**< MVK_CONFIG_ADVERTISE_ROBUST_BUFFER_ACCESS_2 */
 } MVKConfiguration;
 
 // Legacy support for renamed struct elements.
diff --git a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.h b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.h
index ad371c36..a826e626 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.h
+++ b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.h
@@ -20,6 +20,7 @@
 
 #include "MVKCommand.h"
 #include "MVKMTLResourceBindings.h"
+#include "MVKDevice.h"
 #include "MVKSmallVector.h"
 
 #import <Metal/Metal.h>
@@ -193,3 +194,108 @@ protected:
 	uint32_t _drawCount;
 	uint32_t _directCmdFirstInstance;
 };
+
+
+#pragma mark -
+#pragma mark MVKCmdBindTransformFeedbackBuffers
+
+/**
+ * Vulkan command to bind transform feedback (stream output) buffers.
+ * Buffers are tracked in encoder state, but vertex output is not yet captured.
+ * Template class to balance vector pre-allocations between common low counts and fewer larger counts.
+ */
+template <size_t N>
+class MVKCmdBindTransformFeedbackBuffers : public MVKCommand {
+
+public:
+	VkResult setContent(MVKCommandBuffer* cmdBuff,
+						uint32_t firstBinding,
+						uint32_t bindingCount,
+						const VkBuffer* pBuffers,
+						const VkDeviceSize* pOffsets,
+						const VkDeviceSize* pSizes);
+
+	void encode(MVKCommandEncoder* cmdEncoder) override;
+
+protected:
+	MVKCommandTypePool<MVKCommand>* getTypePool(MVKCommandPool* cmdPool) override;
+
+	uint32_t _firstBinding;
+	MVKSmallVector<MVKVertexMTLBufferBinding, N> _bindings;
+};
+
+// Concrete template class implementations.
+typedef MVKCmdBindTransformFeedbackBuffers<4> MVKCmdBindTransformFeedbackBuffers4;
+typedef MVKCmdBindTransformFeedbackBuffers<8> MVKCmdBindTransformFeedbackBuffersMulti;
+
+
+#pragma mark -
+#pragma mark MVKCmdBeginTransformFeedback
+
+class MVKCmdBeginTransformFeedback : public MVKCommand {
+
+public:
+	VkResult setContent(MVKCommandBuffer* cmdBuff,
+						uint32_t firstCounterBuffer,
+						uint32_t counterBufferCount,
+						const VkBuffer* pCounterBuffers,
+						const VkDeviceSize* pCounterBufferOffsets);
+
+	void encode(MVKCommandEncoder* cmdEncoder) override;
+
+protected:
+	MVKCommandTypePool<MVKCommand>* getTypePool(MVKCommandPool* cmdPool) override;
+
+	uint32_t _firstCounterBuffer;
+	MVKSmallVector<MVKVertexMTLBufferBinding, kMVKMaxTransformFeedbackBufferCount> _counterBuffers;
+};
+
+
+#pragma mark -
+#pragma mark MVKCmdEndTransformFeedback
+
+class MVKCmdEndTransformFeedback : public MVKCommand {
+
+public:
+	VkResult setContent(MVKCommandBuffer* cmdBuff,
+						uint32_t firstCounterBuffer,
+						uint32_t counterBufferCount,
+						const VkBuffer* pCounterBuffers,
+						const VkDeviceSize* pCounterBufferOffsets);
+
+	void encode(MVKCommandEncoder* cmdEncoder) override;
+
+protected:
+	MVKCommandTypePool<MVKCommand>* getTypePool(MVKCommandPool* cmdPool) override;
+
+	uint32_t _firstCounterBuffer;
+	MVKSmallVector<MVKVertexMTLBufferBinding, kMVKMaxTransformFeedbackBufferCount> _counterBuffers;
+};
+
+
+#pragma mark -
+#pragma mark MVKCmdDrawIndirectByteCount
+
+class MVKCmdDrawIndirectByteCount : public MVKCommand {
+
+public:
+	VkResult setContent(MVKCommandBuffer* cmdBuff,
+						uint32_t instanceCount,
+						uint32_t firstInstance,
+						VkBuffer counterBuffer,
+						VkDeviceSize counterBufferOffset,
+						uint32_t counterOffset,
+						uint32_t vertexStride);
+
+	void encode(MVKCommandEncoder* cmdEncoder) override;
+
+protected:
+	MVKCommandTypePool<MVKCommand>* getTypePool(MVKCommandPool* cmdPool) override;
+
+	id<MTLBuffer> _mtlCounterBuffer;
+	VkDeviceSize _mtlCounterBufferOffset;
+	uint32_t _instanceCount;
+	uint32_t _firstInstance;
+	uint32_t _counterOffset;
+	uint32_t _vertexStride;
+};
diff --git a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
index 664276bf..49cefbb6 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCmdDraw.mm
@@ -97,7 +97,7 @@
 
 void MVKCmdBindIndexBuffer::encode(MVKCommandEncoder* cmdEncoder) {
     if (_binding.mtlBuffer == nullptr) {
-        // In the null buffer case, offset must be 0, and since we don't support nullDescriptor, the indices are undefined.
+        // In the null buffer case, offset must be 0, and the indices are undefined.
         // Thus, we can use a simple temporary buffer to stand in for the index buffer here.
         const auto* placeholderBuffer = cmdEncoder->getTempMTLBuffer(_binding.size);
         _binding.mtlBuffer = placeholderBuffer->_mtlBuffer;
@@ -1323,3 +1323,150 @@
     }
 }
 
+
+
+#pragma mark -
+#pragma mark MVKCmdBindTransformFeedbackBuffers
+
+template <size_t N>
+VkResult MVKCmdBindTransformFeedbackBuffers<N>::setContent(MVKCommandBuffer* cmdBuff,
+														   uint32_t firstBinding,
+														   uint32_t bindingCount,
+														   const VkBuffer* pBuffers,
+														   const VkDeviceSize* pOffsets,
+														   const VkDeviceSize* pSizes) {
+	_firstBinding = firstBinding;
+	_bindings.clear();	// Clear for reuse
+	_bindings.reserve(bindingCount);
+	MVKVertexMTLBufferBinding b;
+	b.stride = 0;
+	for (uint32_t bindIdx = 0; bindIdx < bindingCount; bindIdx++) {
+		MVKBuffer* mvkBuffer = (MVKBuffer*)pBuffers[bindIdx];
+		b.mtlBuffer = mvkBuffer->getMTLBuffer();
+		b.offset = mvkBuffer->getMTLBufferOffset() + pOffsets[bindIdx];
+		b.size = pSizes ? uint32_t(pSizes[bindIdx] == VK_WHOLE_SIZE ? mvkBuffer->getByteCount() - pOffsets[bindIdx] : pSizes[bindIdx]) : 0;
+		_bindings.push_back(b);
+	}
+
+	return VK_SUCCESS;
+}
+
+template <size_t N>
+void MVKCmdBindTransformFeedbackBuffers<N>::encode(MVKCommandEncoder* cmdEncoder) {
+	cmdEncoder->getState().bindTransformFeedbackBuffers(_firstBinding, _bindings.contents());
+}
+
+template class MVKCmdBindTransformFeedbackBuffers<4>;
+template class MVKCmdBindTransformFeedbackBuffers<8>;
+
+
+#pragma mark -
+#pragma mark MVKCmdBeginTransformFeedback
+
+// Metal has no native transform feedback. Stream output is currently emulated as
+// passthrough: buffers and counter buffers are tracked, but draws execute normally
+// without capturing vertex output. Logged once so apps know capture is not happening.
+static bool _mvkXfbPassthroughLogged = false;
+
+VkResult MVKCmdBeginTransformFeedback::setContent(MVKCommandBuffer* cmdBuff,
+												  uint32_t firstCounterBuffer,
+												  uint32_t counterBufferCount,
+												  const VkBuffer* pCounterBuffers,
+												  const VkDeviceSize* pCounterBufferOffsets) {
+	_firstCounterBuffer = firstCounterBuffer;
+	_counterBuffers.clear();	// Clear for reuse
+	_counterBuffers.reserve(counterBufferCount);
+	MVKVertexMTLBufferBinding b;
+	b.stride = 0;
+	for (uint32_t cbIdx = 0; cbIdx < counterBufferCount; cbIdx++) {
+		MVKBuffer* mvkBuffer = pCounterBuffers ? (MVKBuffer*)pCounterBuffers[cbIdx] : nullptr;
+		if (mvkBuffer) {
+			b.mtlBuffer = mvkBuffer->getMTLBuffer();
+			b.offset = mvkBuffer->getMTLBufferOffset() + (pCounterBufferOffsets ? pCounterBufferOffsets[cbIdx] : 0);
+			b.size = 4;
+		} else {
+			b.mtlBuffer = nullptr;
+			b.offset = 0;
+			b.size = 0;
+		}
+		_counterBuffers.push_back(b);
+	}
+
+	return VK_SUCCESS;
+}
+
+void MVKCmdBeginTransformFeedback::encode(MVKCommandEncoder* cmdEncoder) {
+	if ( !_mvkXfbPassthroughLogged ) {
+		_mvkXfbPassthroughLogged = true;
+		MVKLogWarn("VK_EXT_transform_feedback is emulated as passthrough. Stream output buffers are tracked, but vertex output is not captured.");
+	}
+	cmdEncoder->getState().beginTransformFeedback(_firstCounterBuffer, _counterBuffers.contents());
+}
+
+
+#pragma mark -
+#pragma mark MVKCmdEndTransformFeedback
+
+VkResult MVKCmdEndTransformFeedback::setContent(MVKCommandBuffer* cmdBuff,
+												uint32_t firstCounterBuffer,
+												uint32_t counterBufferCount,
+												const VkBuffer* pCounterBuffers,
+												const VkDeviceSize* pCounterBufferOffsets) {
+	_firstCounterBuffer = firstCounterBuffer;
+	_counterBuffers.clear();	// Clear for reuse
+	_counterBuffers.reserve(counterBufferCount);
+	MVKVertexMTLBufferBinding b;
+	b.stride = 0;
+	for (uint32_t cbIdx = 0; cbIdx < counterBufferCount; cbIdx++) {
+		MVKBuffer* mvkBuffer = pCounterBuffers ? (MVKBuffer*)pCounterBuffers[cbIdx] : nullptr;
+		if (mvkBuffer) {
+			b.mtlBuffer = mvkBuffer->getMTLBuffer();
+			b.offset = mvkBuffer->getMTLBufferOffset() + (pCounterBufferOffsets ? pCounterBufferOffsets[cbIdx] : 0);
+			b.size = 4;
+		} else {
+			b.mtlBuffer = nullptr;
+			b.offset = 0;
+			b.size = 0;
+		}
+		_counterBuffers.push_back(b);
+	}
+
+	return VK_SUCCESS;
+}
+
+void MVKCmdEndTransformFeedback::encode(MVKCommandEncoder* cmdEncoder) {
+	cmdEncoder->getState().endTransformFeedback();
+}
+
+
+#pragma mark -
+#pragma mark MVKCmdDrawIndirectByteCount
+
+VkResult MVKCmdDrawIndirectByteCount::setContent(MVKCommandBuffer* cmdBuff,
+												 uint32_t instanceCount,
+												 uint32_t firstInstance,
+												 VkBuffer counterBuffer,
+												 VkDeviceSize counterBufferOffset,
+												 uint32_t counterOffset,
+												 uint32_t vertexStride) {
+	_instanceCount = instanceCount;
+	_firstInstance = firstInstance;
+	_counterOffset = counterOffset;
+	_vertexStride = vertexStride;
+
+	MVKBuffer* mvkBuffer = (MVKBuffer*)counterBuffer;
+	_mtlCounterBuffer = mvkBuffer->getMTLBuffer();
+	_mtlCounterBufferOffset = mvkBuffer->getMTLBufferOffset() + counterBufferOffset;
+
+	return VK_SUCCESS;
+}
+
+void MVKCmdDrawIndirectByteCount::encode(MVKCommandEncoder* cmdEncoder) {
+	// The draw count depends on the stream output counter, which is not tracked
+	// in the current passthrough emulation, so this draw cannot be performed.
+	static bool _mvkDrawIndirectByteCountLogged = false;
+	if ( !_mvkDrawIndirectByteCountLogged ) {
+		_mvkDrawIndirectByteCountLogged = true;
+		MVKLogWarn("vkCmdDrawIndirectByteCountEXT is not supported while stream output is emulated as passthrough. Draw skipped.");
+	}
+}
diff --git a/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.h b/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.h
index 25e6017b..089a5362 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.h
+++ b/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.h
@@ -182,10 +182,13 @@ struct MVKVulkanGraphicsCommandEncoderState: public MVKVulkanCommonEncoderState
 	MVKRenderStateData _renderState;
 	MVKVertexMTLBufferBinding _vertexBuffers[kMVKMaxBufferCount];
 	MVKIndexMTLBufferBinding _indexBuffer;
+	MVKVertexMTLBufferBinding _xfbBuffers[kMVKMaxTransformFeedbackBufferCount];
+	MVKVertexMTLBufferBinding _xfbCounterBuffers[kMVKMaxTransformFeedbackBufferCount];
 	VkViewport _viewports[kMVKMaxViewportScissorCount];
 	VkRect2D _scissors[kMVKMaxViewportScissorCount];
 	MTLSamplePosition _sampleLocations[kMVKMaxSampleCount];
 	MVKImplicitBufferData _implicitBufferData[kMVKShaderStageFragment + 1];
+	bool _transformFeedbackActive = false;
 
 	/** Choose between the dynamic and pipeline render states based on whether the given state flag is marked dynamic on the pipeline. */
 	const MVKRenderStateData& pickRenderState(MVKRenderStateFlag state) const {
@@ -499,6 +502,12 @@ public:
 	void bindVertexBuffers(uint32_t firstBinding, MVKArrayRef<const MVKVertexMTLBufferBinding> buffers);
 	/** Binds the given index buffer to the Vulkan state, invalidating any necessary resources. */
 	void bindIndexBuffer(const MVKIndexMTLBufferBinding& buffer);
+	/** Binds the given transform feedback (stream output) buffers to the Vulkan state. Buffers are tracked but vertex output is not yet captured. */
+	void bindTransformFeedbackBuffers(uint32_t firstBinding, MVKArrayRef<const MVKVertexMTLBufferBinding> buffers);
+	/** Begins transform feedback, binding the given counter buffers. Tracked but not yet captured. */
+	void beginTransformFeedback(uint32_t firstCounterBuffer, MVKArrayRef<const MVKVertexMTLBufferBinding> counterBuffers);
+	/** Ends transform feedback. */
+	void endTransformFeedback();
 	void offsetZeroDivisorVertexBuffers(MVKCommandEncoder& mvkEncoder, MVKGraphicsStage stage, MVKGraphicsPipeline* pipeline, uint32_t firstInstance);
 
 	/** Begins tracking for a fresh MTLRenderCommandEncoder. */
diff --git a/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm b/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm
index 7d16b94e..6a76e3c3 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm
+++ b/MoltenVK/MoltenVK/Commands/MVKCommandEncoderState.mm
@@ -480,7 +480,10 @@ static void executeBindOp(id<MTLCommandEncoder> encoder,
 						binder.setBuffer(encoder, buffer, offset, target + i);
 					}
 				} else {
-					bindBuffer(encoder, static_cast<id<MTLBuffer>>(resource), offset, target + i, exists, bindings, binder);
+					// A null buffer descriptor binds a zeroed stand-in buffer, so that reads
+					// return zero and writes are discarded, per nullDescriptor semantics.
+					id<MTLBuffer> buffer = resource ? static_cast<id<MTLBuffer>>(resource) : dev->getNullDescriptorMTLBuffer();
+					bindBuffer(encoder, buffer, offset, target + i, exists, bindings, binder);
 				}
 				break;
 			}
@@ -504,7 +507,8 @@ static void executeBindOp(id<MTLCommandEncoder> encoder,
 				break;
 
 			case MVKDescriptorBindOperationCode::BindSampler:
-				bindSampler(encoder, static_cast<id<MTLSamplerState>>(resource), target + i, exists, bindings, binder);
+				// A null sampler descriptor binds the default sampler, per nullDescriptor semantics.
+				bindSampler(encoder, resource ? static_cast<id<MTLSamplerState>>(resource) : dev->getDefaultMTLSamplerState(), target + i, exists, bindings, binder);
 				break;
 
 			case MVKDescriptorBindOperationCode::BindSamplerWithLiveCheck:
@@ -1812,6 +1816,23 @@ static void invalidateImplicitBuffer(MVKCommandEncoderState& state, VkPipelineBi
 #endif
 }
 
+void MVKCommandEncoderState::bindTransformFeedbackBuffers(uint32_t firstBinding, MVKArrayRef<const MVKVertexMTLBufferBinding> buffers) {
+	uint32_t end = std::min(firstBinding + (uint32_t)buffers.size(), kMVKMaxTransformFeedbackBufferCount);
+	for (uint32_t i = firstBinding; i < end; i++)
+		_vkGraphics._xfbBuffers[i] = buffers[i - firstBinding];
+}
+
+void MVKCommandEncoderState::beginTransformFeedback(uint32_t firstCounterBuffer, MVKArrayRef<const MVKVertexMTLBufferBinding> counterBuffers) {
+	uint32_t end = std::min(firstCounterBuffer + (uint32_t)counterBuffers.size(), kMVKMaxTransformFeedbackBufferCount);
+	for (uint32_t i = firstCounterBuffer; i < end; i++)
+		_vkGraphics._xfbCounterBuffers[i] = counterBuffers[i - firstCounterBuffer];
+	_vkGraphics._transformFeedbackActive = true;
+}
+
+void MVKCommandEncoderState::endTransformFeedback() {
+	_vkGraphics._transformFeedbackActive = false;
+}
+
 void MVKCommandEncoderState::offsetZeroDivisorVertexBuffers(MVKCommandEncoder& mvkEncoder, MVKGraphicsStage stage, MVKGraphicsPipeline* pipeline, uint32_t firstInstance) {
 	for (const auto& binding : pipeline->getZeroDivisorVertexBindings()) {
 		uint32_t mtlBuffIdx = pipeline->getMetalBufferIndexForVertexAttributeBinding(binding.first);
diff --git a/MoltenVK/MoltenVK/Commands/MVKCommandTypePools.def b/MoltenVK/MoltenVK/Commands/MVKCommandTypePools.def
index 423fef14..527eef9e 100644
--- a/MoltenVK/MoltenVK/Commands/MVKCommandTypePools.def
+++ b/MoltenVK/MoltenVK/Commands/MVKCommandTypePools.def
@@ -119,6 +119,10 @@ MVK_CMD_TYPE_POOL(Draw)
 MVK_CMD_TYPE_POOL(DrawIndexed)
 MVK_CMD_TYPE_POOL(DrawIndirect)
 MVK_CMD_TYPE_POOL(DrawIndexedIndirect)
+MVK_CMD_TYPE_POOLS_FROM_THRESHOLD(BindTransformFeedbackBuffers, 4)
+MVK_CMD_TYPE_POOL(BeginTransformFeedback)
+MVK_CMD_TYPE_POOL(EndTransformFeedback)
+MVK_CMD_TYPE_POOL(DrawIndirectByteCount)
 MVK_CMD_TYPE_POOLS_FROM_THRESHOLD(CopyImage, 1)
 MVK_CMD_TYPE_POOLS_FROM_THRESHOLD(BlitImage, 1)
 MVK_CMD_TYPE_POOLS_FROM_THRESHOLD(ResolveImage, 1)
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm b/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm
index 3cdc6234..4661f13a 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKDescriptorSet.mm
@@ -901,7 +901,10 @@ static bool canUseFastPathUpdate(MVKDescriptorGPULayout layout, MVKArgumentBuffe
 /** Argument buffer encoder for Metal 3 encoding */
 template <> struct MVKArgBufEncoder<MVKArgumentBufferMode::Metal3> {
 	MVKGPUResource* dst;
-	MVKArgBufEncoder(id<MTLArgumentEncoder> enc, char* base): dst(reinterpret_cast<MVKGPUResource*>(base)) {}
+	MTLResourceID nullSamp;
+	uint64_t nullBufAddr;
+	MVKArgBufEncoder(id<MTLArgumentEncoder> enc, char* base, id<MTLSamplerState> nullSamp_, id<MTLBuffer> nullBuf):
+		dst(reinterpret_cast<MVKGPUResource*>(base)), nullSamp(nullSamp_.gpuResourceID), nullBufAddr(nullBuf.gpuAddress) {}
 	void advance(size_t stride) { dst = reinterpret_cast<MVKGPUResource*>(reinterpret_cast<char*>(dst) + stride); }
 	void* constantData(size_t index) { return reinterpret_cast<char*>(dst) + index; }
 	void setTexture(id<MTLTexture> tex,       size_t index = 0) { dst[index].resource = tex.gpuResourceID; }
@@ -910,12 +913,14 @@ void setBuffer(id<MTLBuffer> buf, uint64_t offset, size_t index = 0) {
 		dst[index].gpuAddress = buf.gpuAddress + offset;
 	}
 	void setNullTexture(size_t index = 0) { dst[index].resource = {}; }
-	void setNullSampler(size_t index = 0) { dst[index].resource = {}; }
-	void setNullBuffer (size_t index = 0) { dst[index].gpuAddress = 0; }
+	void setNullSampler(size_t index = 0) { dst[index].resource = nullSamp; }
+	void setNullBuffer (size_t index = 0) { dst[index].gpuAddress = nullBufAddr; }
 	void setTexture(MVKImageView* img, size_t index = 0) { setTexture(img ? img->getMTLTexture() : nil, index); }
-	void setSampler(MVKSampler* samp, size_t index = 0) { setSampler(samp ? samp->getMTLSamplerState() : nil, index); }
+	void setSampler(MVKSampler* samp, size_t index = 0) {
+		if (samp) { setSampler(samp->getMTLSamplerState(), index); } else { setNullSampler(index); }
+	}
 	void setBuffer(const VkDescriptorBufferInfo* info, size_t index = 0) {
-		uint64_t addr = 0;
+		uint64_t addr = nullBufAddr;
 		if (MVKBuffer* buf = reinterpret_cast<MVKBuffer*>(info->buffer))
 			addr = buf->getMTLBuffer().gpuAddress + buf->getMTLBufferOffset() + info->offset;
 		dst[index].gpuAddress = addr;
@@ -925,8 +930,11 @@ void setBuffer(const VkDescriptorBufferInfo* info, size_t index = 0) {
 /** Argument buffer encoder for argument encoder encoding */
 template <> struct MVKArgBufEncoder<MVKArgumentBufferMode::ArgEncoder> {
 	id<MTLArgumentEncoder> enc;
+	id<MTLSamplerState> nullSamp;
+	id<MTLBuffer> nullBuf;
 	size_t base;
-	MVKArgBufEncoder(id<MTLArgumentEncoder> enc_, char* base): enc(enc_), base(0) {}
+	MVKArgBufEncoder(id<MTLArgumentEncoder> enc_, char* base, id<MTLSamplerState> nullSamp_, id<MTLBuffer> nullBuf_):
+		enc(enc_), nullSamp(nullSamp_), nullBuf(nullBuf_), base(0) {}
 	void advance(size_t stride) { base += stride; }
 	void* constantData(uint32_t index) { return [enc constantDataAtIndex:base + index]; }
 	void setTexture(id<MTLTexture> tex,       size_t index = 0) { [enc setTexture:tex atIndex:base + index]; }
@@ -935,13 +943,13 @@ void setBuffer(id<MTLBuffer> buf, uint64_t offset, size_t index = 0) {
 		[enc setBuffer:buf offset:static_cast<NSUInteger>(offset) atIndex:base + index];
 	}
 	void setNullTexture(size_t index = 0) { [enc setTexture:nil atIndex:base + index]; }
-	void setNullSampler(size_t index = 0) { [enc setSamplerState:nil atIndex:base + index]; }
-	void setNullBuffer (size_t index = 0) { [enc setBuffer:nil offset:0 atIndex:base + index]; }
+	void setNullSampler(size_t index = 0) { [enc setSamplerState:nullSamp atIndex:base + index]; }
+	void setNullBuffer (size_t index = 0) { [enc setBuffer:nullBuf offset:0 atIndex:base + index]; }
 	void setTexture(MVKImageView* img, size_t index = 0) {
 		[enc setTexture:img ? img->getMTLTexture() : nil atIndex:base + index];
 	}
 	void setBuffer(const VkDescriptorBufferInfo* info, size_t index = 0) {
-		id<MTLBuffer> mtlbuf = nil;
+		id<MTLBuffer> mtlbuf = nullBuf;
 		NSUInteger offset = 0;
 		if (MVKBuffer* buf = reinterpret_cast<MVKBuffer*>(info->buffer)) {
 			mtlbuf = buf->getMTLBuffer();
@@ -950,7 +958,7 @@ void setBuffer(const VkDescriptorBufferInfo* info, size_t index = 0) {
 		[enc setBuffer:mtlbuf offset:offset atIndex:base + index];
 	}
 	void setSampler(MVKSampler* samp, size_t index = 0) {
-		[enc setSamplerState:samp ? samp->getMTLSamplerState() : nil atIndex:base + index];
+		if (samp) { setSampler(samp->getMTLSamplerState(), index); } else { setNullSampler(index); }
 	}
 };
 
@@ -964,6 +972,7 @@ static constexpr size_t descriptorGPUStride(MVKArgumentBufferMode argBufMode, MV
 
 template <MVKArgumentBufferMode ArgBufMode, MVKDescriptorGPULayout Layout>
 static void writeDescriptorSetGPUBuffer(
+	MVKDevice* device,
 	const MVKDescriptorBinding& binding,
 	id<MTLArgumentEncoder> enc_, char* base, const uint32_t* auxOffsets,
 	const void* src, size_t srcStride, MVKDescriptorUpdateSourceType srcType,
@@ -971,7 +980,7 @@ static void writeDescriptorSetGPUBuffer(
 {
 	constexpr size_t dstStride = descriptorGPUStride(ArgBufMode, Layout);
 	size_t startOffset = start * dstStride;
-	MVKArgBufEncoder<ArgBufMode> enc(enc_, base);
+	MVKArgBufEncoder<ArgBufMode> enc(enc_, base, device->getDefaultMTLSamplerState(), device->getNullDescriptorMTLBuffer());
 	uint32_t baseOffset = ArgBufMode == MVKArgumentBufferMode::ArgEncoder ? binding.argBufID : binding.gpuOffset;
 
 	if (Layout == MVKDescriptorGPULayout::OutlinedData || Layout == MVKDescriptorGPULayout::InlineData) {
@@ -1083,6 +1092,7 @@ static void advanceBinding(const MVKDescriptorBinding** binding) {
 
 template <MVKArgumentBufferMode ArgBufMode>
 static void writeDescriptorSetGPUBuffer(
+	MVKDevice* device,
 	const MVKDescriptorBinding* binding, const MVKDescriptorSet* set,
 	const void* src, size_t srcStride, MVKDescriptorUpdateSourceType srcType,
 	id<MTLArgumentEncoder> enc,
@@ -1092,7 +1102,7 @@ static void writeDescriptorSetGPUBuffer(
 	const uint32_t* auxOffsets = set->auxIndices;
 	if (canUseFastPathUpdate(binding->gpuLayout, ArgBufMode)) {
 		switch (binding->gpuLayout) {
-#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(*binding, enc, base, auxOffsets, src, srcStride, srcType, start, count)
+#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(device, *binding, enc, base, auxOffsets, src, srcStride, srcType, start, count)
 			case MVKDescriptorGPULayout::None:          break;
 			case MVKDescriptorGPULayout::Texture:       DISPATCH(Texture);       break;
 			case MVKDescriptorGPULayout::Sampler:       DISPATCH(Sampler);       break;
@@ -1122,7 +1132,7 @@ static void writeDescriptorSetGPUBuffer(
 		while (true) {
 			uint32_t numWrite = std::min(count, binding->descriptorCount - start);
 			switch (binding->gpuLayout) {
-#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(*binding, enc, base, auxOffsets, src, srcStride, srcType, start, numWrite)
+#define DISPATCH(x) writeDescriptorSetGPUBuffer<ArgBufMode, MVKDescriptorGPULayout::x>(device, *binding, enc, base, auxOffsets, src, srcStride, srcType, start, numWrite)
 				case MVKDescriptorGPULayout::TexBufSoA:     DISPATCH(TexBufSoA);    break;
 				case MVKDescriptorGPULayout::TexSampSoA:    DISPATCH(TexSampSoA);   break;
 				case MVKDescriptorGPULayout::Tex2SampSoA:   DISPATCH(Tex2SampSoA);  break;
@@ -1368,14 +1378,15 @@ static void writeDescriptorSetBinding(
 {
 	char* cpuBuffer = set->cpuBuffer + binding->cpuOffset;
 	writeDescriptorSetCPUBufferDispatch(layout, *binding, cpuBuffer, src, stride, type, start, count);
+	MVKDevice* device = layout->getDevice();
 	switch (layout->argBufMode()) {
 		case MVKArgumentBufferMode::Off:
 			break; // No GPU buffer
 		case MVKArgumentBufferMode::ArgEncoder:
-			writeDescriptorSetGPUBuffer<MVKArgumentBufferMode::ArgEncoder>(binding, set, src, stride, type, enc, start, count);
+			writeDescriptorSetGPUBuffer<MVKArgumentBufferMode::ArgEncoder>(device, binding, set, src, stride, type, enc, start, count);
 			break;
 		case MVKArgumentBufferMode::Metal3:
-			writeDescriptorSetGPUBuffer<MVKArgumentBufferMode::Metal3    >(binding, set, src, stride, type, enc, start, count);
+			writeDescriptorSetGPUBuffer<MVKArgumentBufferMode::Metal3    >(device, binding, set, src, stride, type, enc, start, count);
 			break;
 	}
 }
@@ -1397,15 +1408,16 @@ static void copyArgBuf(MVKDevice* device, id<MTLArgumentEncoder> enc,
 			[enc setTexture:tex && device->getLiveResources().isLive(tex) ? tex : nil atIndex:start + i];
 		} else if constexpr (Layout == MVKDescriptorGPULayout::Sampler) {
 			id<MTLSamplerState> samp = *reinterpret_cast<const id<MTLSamplerState>*>(src);
-			[enc setSamplerState:samp && device->getLiveResources().isLive(samp) ? samp : nil atIndex:start + i];
+			bool live = samp && device->getLiveResources().isLive(samp);
+			[enc setSamplerState:live ? samp : device->getDefaultMTLSamplerState() atIndex:start + i];
 		} else if constexpr (Layout == MVKDescriptorGPULayout::Buffer) {
 			id<MTLBuffer> buf = *reinterpret_cast<const id<MTLBuffer>*>(src);
 			uint64_t offset = *reinterpret_cast<const uint64_t*>(src + bufferOffsetOffset);
 			if (buf) {
 				auto live = device->getLiveResources().isLive(buf);
-				[enc setBuffer:live ? buf : nil offset:live ? offset : 0 atIndex:start + i];
+				[enc setBuffer:live ? buf : device->getNullDescriptorMTLBuffer() offset:live ? offset : 0 atIndex:start + i];
 			} else {
-				[enc setBuffer:nil offset:0 atIndex:start + i];
+				[enc setBuffer:device->getNullDescriptorMTLBuffer() offset:0 atIndex:start + i];
 			}
 		} else {
 			static_assert(Layout != Layout, "Other layouts are unsupported");
@@ -2145,10 +2157,30 @@ static VkResult pickOOMError(const MVKDescriptorPoolFreeList& free, MVKArrayRef<
 	if (set->gpuBuffer)
 		memset(set->gpuBuffer, 0, gpuSize);
 
-	if (mvkDSL->numAuxOffsets() || mvkDSL->immutableSamplers().size() != 0) {
-		// Need to write aux buffer pointers and immutable samplers to GPU buffer
+	// Determine whether any bindings contain buffer or sampler descriptors, whose
+	// never-written null state must be initialized to safe stand-ins that return
+	// zero on read, as required by VkPhysicalDeviceRobustness2FeaturesEXT::nullDescriptor.
+	bool hasNullDescriptors = false;
+	for (const auto& binding : mvkDSL->bindings()) {
+		switch (binding.gpuLayout) {
+			case MVKDescriptorGPULayout::Sampler:
+			case MVKDescriptorGPULayout::Buffer:
+			case MVKDescriptorGPULayout::BufferAuxSize:
+			case MVKDescriptorGPULayout::TexBufSoA:
+			case MVKDescriptorGPULayout::TexSampSoA:
+				hasNullDescriptors = true;
+				break;
+			default:
+				break;
+		}
+	}
+
+	if (mvkDSL->numAuxOffsets() || mvkDSL->immutableSamplers().size() != 0 || hasNullDescriptors) {
+		// Need to write aux buffer pointers, immutable samplers, and null descriptor stand-ins to GPU buffer
 		const uint32_t* indices = set->auxIndices;
 		uint32_t baseOffset = set->gpuBufferOffset;
+		id<MTLSamplerState> nullSamp = getDevice()->getDefaultMTLSamplerState();
+		id<MTLBuffer> nullBuf = getDevice()->getNullDescriptorMTLBuffer();
 		switch (mvkDSL->argBufMode()) {
 			case MVKArgumentBufferMode::ArgEncoder: {
 				id<MTLBuffer> buffer = _gpuBufferObject;
@@ -2158,35 +2190,90 @@ static VkResult pickOOMError(const MVKDescriptorPoolFreeList& free, MVKArrayRef<
 				if (mvkDSL->needsSizeBuf())
 					[enc setBuffer:buffer offset:baseOffset + gpuBase atIndex:0];
 				for (const auto& binding : mvkDSL->bindings()) {
+					uint32_t count = binding.getDescriptorCount(set->variableDescriptorCount);
 					if (binding.gpuLayout == MVKDescriptorGPULayout::OutlinedData)
 						[enc setBuffer:buffer offset:baseOffset + indices[binding.auxIndex] atIndex:binding.argBufID];
 					if (binding.hasImmutableSamplers()) {
 						// SPIRV-Cross doesn't use constexpr samplers with argument buffers, so we need to bind them.
-						uint32_t count = binding.descriptorCount;
 						uint32_t base = binding.argBufID + descriptorTextureCount(binding.gpuLayout) * count;
 						MVKSampler*const* samp = &mvkDSL->immutableSamplers()[binding.immSamplerIndex];
 						for (uint32_t i = 0; i < count; i++)
 							[enc setSamplerState:samp[i]->getMTLSamplerState() atIndex:base + i];
 					}
+					switch (binding.gpuLayout) {
+						case MVKDescriptorGPULayout::Sampler:
+							if (!binding.hasImmutableSamplers())
+								for (uint32_t i = 0; i < count; i++)
+									[enc setSamplerState:nullSamp atIndex:binding.argBufID + i];
+							break;
+						case MVKDescriptorGPULayout::Buffer:
+						case MVKDescriptorGPULayout::BufferAuxSize:
+							for (uint32_t i = 0; i < count; i++)
+								[enc setBuffer:nullBuf offset:0 atIndex:binding.argBufID + i];
+							break;
+						case MVKDescriptorGPULayout::TexBufSoA:
+							for (uint32_t i = 0; i < count; i++)
+								[enc setBuffer:nullBuf offset:0 atIndex:binding.argBufID + count + i];
+							break;
+						case MVKDescriptorGPULayout::TexSampSoA:
+							for (uint32_t i = 0; i < count; i++)
+								[enc setSamplerState:nullSamp atIndex:binding.argBufID + count + i];
+							break;
+						default:
+							break;
+					}
 				}
 				break;
 			}
 			case MVKArgumentBufferMode::Metal3: {
 				uint64_t buffer = _gpuBufferGPUAddress + baseOffset;
+				uint64_t nullBufAddr = nullBuf.gpuAddress;
+				MTLResourceID nullSampID = nullSamp.gpuResourceID;
 				char* base = set->gpuBuffer;
 				if (mvkDSL->needsSizeBuf())
 					*reinterpret_cast<uint64_t*>(base) = buffer + gpuBase;
 				for (const auto& binding : mvkDSL->bindings()) {
+					uint32_t count = binding.getDescriptorCount(set->variableDescriptorCount);
 					if (binding.gpuLayout == MVKDescriptorGPULayout::OutlinedData)
 						*reinterpret_cast<uint64_t*>(base + binding.gpuOffset) = buffer + indices[binding.auxIndex];
 					if (binding.hasImmutableSamplers()) {
 						// SPIRV-Cross doesn't use constexpr samplers with argument buffers, so we need to bind them.
-						uint32_t count = binding.descriptorCount;
 						MTLResourceID* write = reinterpret_cast<MTLResourceID*>(base + binding.gpuOffset) + descriptorTextureCount(binding.gpuLayout) * count;
 						MVKSampler*const* samp = &mvkDSL->immutableSamplers()[binding.immSamplerIndex];
 						for (uint32_t i = 0; i < count; i++)
 							write[i] = samp[i]->getMTLSamplerState().gpuResourceID;
 					}
+					switch (binding.gpuLayout) {
+						case MVKDescriptorGPULayout::Sampler: {
+							if (!binding.hasImmutableSamplers()) {
+								MTLResourceID* write = reinterpret_cast<MTLResourceID*>(base + binding.gpuOffset);
+								for (uint32_t i = 0; i < count; i++)
+									write[i] = nullSampID;
+							}
+							break;
+						}
+						case MVKDescriptorGPULayout::Buffer:
+						case MVKDescriptorGPULayout::BufferAuxSize: {
+							uint64_t* write = reinterpret_cast<uint64_t*>(base + binding.gpuOffset);
+							for (uint32_t i = 0; i < count; i++)
+								write[i] = nullBufAddr;
+							break;
+						}
+						case MVKDescriptorGPULayout::TexBufSoA: {
+							uint64_t* write = reinterpret_cast<uint64_t*>(base + binding.gpuOffset) + count;
+							for (uint32_t i = 0; i < count; i++)
+								write[i] = nullBufAddr;
+							break;
+						}
+						case MVKDescriptorGPULayout::TexSampSoA: {
+							MTLResourceID* write = reinterpret_cast<MTLResourceID*>(base + binding.gpuOffset) + count;
+							for (uint32_t i = 0; i < count; i++)
+								write[i] = nullSampID;
+							break;
+						}
+						default:
+							break;
+					}
 				}
 				break;
 			}
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h
index 534a414f..fd08d6c9 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.h
@@ -88,6 +88,7 @@ static constexpr uint32_t   kMVKMaxViewportScissorCount = 16;
 static constexpr uint32_t   kMVKMaxDescriptorSetCount = SPIRV_CROSS_NAMESPACE::kMaxArgumentBuffers;
 static constexpr uint32_t   kMVKMaxTextureCount = 128; // Maximum value across all GPUs in Metal feature set tables
 static constexpr uint32_t   kMVKMaxBufferCount = 31;
+static constexpr uint32_t   kMVKMaxTransformFeedbackBufferCount = 4;	// Vulkan spec minimum for maxTransformFeedbackBuffers
 static constexpr uint32_t   kMVKMaxSamplerCount = 16;
 static constexpr uint32_t   kMVKMaxSampleCount = 8;
 static constexpr uint32_t   kMVKSampleLocationCoordinateGridSize = 16;
@@ -496,6 +497,7 @@ protected:
 	void populateDeviceIDProperties(VkPhysicalDeviceVulkan11Properties* pVk11Props);
 	void populateSubgroupProperties(VkPhysicalDeviceVulkan11Properties* pVk11Props);
 	template<typename HostImageCopyProps> void populateHostImageCopyProperties(HostImageCopyProps* pHostImageCopyProps);
+	void populateTransformFeedbackProperties(VkPhysicalDeviceTransformFeedbackPropertiesEXT* pXFBProps);
 	bool isTier2MetalArgumentBuffers();
 	void logGPUInfo();
 
@@ -943,6 +945,12 @@ public:
 	 */
 	id<MTLBuffer> getDummyBlitMTLBuffer();
 
+	/**
+	 * Returns a small zeroed MTLBuffer that is bound in place of null buffer descriptors,
+	 * so that reads through a null buffer descriptor return zero, and writes are discarded safely.
+	 */
+	id<MTLBuffer> getNullDescriptorMTLBuffer();
+
 	/**
 	 * Returns whether MTLCommandBuffers can be prefilled.
 	 *
@@ -1111,6 +1119,7 @@ protected:
 	std::string _capturePipeFileName;
 	id<MTLSamplerState> _defaultMTLSamplerState = nil;
 	id<MTLBuffer> _dummyBlitMTLBuffer = nil;
+	id<MTLBuffer> _nullDescriptorMTLBuffer = nil;
 #if MVK_XCODE_16
 	id<MTLResidencySet> _residencySet = nil;
 #endif
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm
index 97727575..c8e998a1 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKDevice.mm
@@ -633,9 +633,15 @@
 			}
 			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_ROBUSTNESS_2_FEATURES_KHR: {
 				auto* robustness2Features = (VkPhysicalDeviceRobustness2FeaturesKHR*)next;
-				robustness2Features->robustBufferAccess2 = false;
+				// Metal does not bounds-check shader buffer accesses, so robustBufferAccess2
+				// relies on Apple GPU memory containment in practice, and on shader-level bounds
+				// behaviour applied by translation layers such as vkd3d-proton. It can be disabled
+				// via the MVK_CONFIG_ADVERTISE_ROBUST_BUFFER_ACCESS_2 config setting.
+				robustness2Features->robustBufferAccess2 = _gpuCapabilities.isAppleGPU && getMVKConfig().advertiseRobustBufferAccess2;
 				robustness2Features->robustImageAccess2 = _gpuCapabilities.isAppleGPU;
-				robustness2Features->nullDescriptor = false;
+				// Null descriptors are encoded as nil textures (which Metal reads as zero),
+				// the default sampler, or a small zeroed stand-in buffer.
+				robustness2Features->nullDescriptor = true;
 				break;
 			}
 			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_FMA_FEATURES_KHR: {
@@ -786,6 +792,20 @@
 				texelBuffAlignFeatures->texelBufferAlignment = true;
 				break;
 			}
+			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_FEATURES_EXT: {
+				// Metal has no native transform feedback and VKMT does not yet provide
+				// capture emulation. Do not expose a feature bit for a passthrough path.
+				auto* xfbFeatures = (VkPhysicalDeviceTransformFeedbackFeaturesEXT*)next;
+				xfbFeatures->transformFeedback = false;
+				xfbFeatures->geometryStreams = false;
+				break;
+			}
+			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_PROPERTIES_EXT: {
+				// This is a properties struct, but some apps (eg. vkd3d-proton) chain it
+				// unconditionally, including into a VkPhysicalDeviceFeatures2 query.
+				populateTransformFeedbackProperties((VkPhysicalDeviceTransformFeedbackPropertiesEXT*)next);
+				break;
+			}
 			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SHADER_INTEGER_FUNCTIONS_2_FEATURES_INTEL: {
 				auto* shaderIntFuncsFeatures = (VkPhysicalDeviceShaderIntegerFunctions2FeaturesINTEL*)next;
 				shaderIntFuncsFeatures->shaderIntegerFunctions2 = true;
@@ -1293,6 +1313,10 @@
 				provokingVertexProps->transformFeedbackPreservesTriangleFanProvokingVertex = false;
 				break;
 			}
+			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_TRANSFORM_FEEDBACK_PROPERTIES_EXT: {
+				populateTransformFeedbackProperties((VkPhysicalDeviceTransformFeedbackPropertiesEXT*)next);
+				break;
+			}
 			case VK_STRUCTURE_TYPE_PHYSICAL_DEVICE_SAMPLE_LOCATIONS_PROPERTIES_EXT: {
 				auto* sampLocnProps = (VkPhysicalDeviceSampleLocationsPropertiesEXT*)next;
 				sampLocnProps->sampleLocationSampleCounts = _metalFeatures.supportedSamplePosCounts;
@@ -1315,6 +1339,20 @@
 	}
 }
 
+void MVKPhysicalDevice::populateTransformFeedbackProperties(VkPhysicalDeviceTransformFeedbackPropertiesEXT* pXFBProps) {
+	// Metal has no native transform feedback and capture emulation is not ready.
+	// Keep all advertised limits and behavior bits disabled until a direct
+	// output/counter/query contract passes.
+	pXFBProps->maxTransformFeedbackBuffers = 0;
+	pXFBProps->maxTransformFeedbackBufferSize = 0;
+	pXFBProps->maxTransformFeedbackStreamDataSize = 0;
+	pXFBProps->maxTransformFeedbackBufferDataSize = 0;
+	pXFBProps->maxTransformFeedbackBufferDataStride = 0;
+	pXFBProps->transformFeedbackQueries = false;
+	pXFBProps->transformFeedbackStreamsLinesTriangles = false;
+	pXFBProps->transformFeedbackRasterizationStreamSelect = false;
+	pXFBProps->transformFeedbackDraw = false;
+}
+
 template<typename HostImageCopyProps>
 void MVKPhysicalDevice::populateHostImageCopyProperties(HostImageCopyProps* pHostImageCopyProps) {
 
@@ -4923,6 +4961,24 @@ static uint32_t mvkGetEntryProperty(io_registry_entry_t entry, CFStringRef prope
 	return _dummyBlitMTLBuffer;
 }
 
+/** The size of the zeroed buffer bound in place of null buffer descriptors. */
+static constexpr NSUInteger kMVKNullDescriptorMTLBufferSize = 256;
+
+id<MTLBuffer> MVKDevice::getNullDescriptorMTLBuffer() {
+	if ( !_nullDescriptorMTLBuffer ) {
+
+		// Lock and check again in case another thread has created the buffer.
+		lock_guard<mutex> lock(_rezLock);
+		if ( !_nullDescriptorMTLBuffer ) {
+			@autoreleasepool {
+				_nullDescriptorMTLBuffer = [_physicalDevice->_mtlDevice newBufferWithLength: kMVKNullDescriptorMTLBufferSize options: MTLResourceStorageModeShared];	// retained
+				memset(_nullDescriptorMTLBuffer.contents, 0, kMVKNullDescriptorMTLBufferSize);
+			}
+		}
+	}
+	return _nullDescriptorMTLBuffer;
+}
+
 MTLCompileOptions* MVKDevice::getMTLCompileOptions(uint32_t fpFastMathFlags,
 												   bool preserveInvariance) {
 	MTLCompileOptions* mtlCompOpt = [MTLCompileOptions new];
@@ -5557,6 +5613,7 @@ static uint32_t mvkGetEntryProperty(io_registry_entry_t entry, CFStringRef prope
 #endif
 	[_defaultMTLSamplerState release];
 	[_dummyBlitMTLBuffer release];
+	[_nullDescriptorMTLBuffer release];
 
 	stopAutoGPUCapture(MVK_CONFIG_AUTO_GPU_CAPTURE_SCOPE_DEVICE);
 
diff --git a/MoltenVK/MoltenVK/GPUObjects/MVKInstance.mm b/MoltenVK/MoltenVK/GPUObjects/MVKInstance.mm
index 757fe164..a3ea806b 100644
--- a/MoltenVK/MoltenVK/GPUObjects/MVKInstance.mm
+++ b/MoltenVK/MoltenVK/GPUObjects/MVKInstance.mm
@@ -818,6 +818,10 @@
 	ADD_DVC_EXT_ENTRY_POINT(vkCmdSetTessellationDomainOriginEXT, EXT_EXTENDED_DYNAMIC_STATE_3);
 	ADD_DVC_EXT_ENTRY_POINT(vkGetMemoryMetalHandleEXT, EXT_EXTERNAL_MEMORY_METAL);
 	ADD_DVC_EXT_ENTRY_POINT(vkGetMemoryMetalHandlePropertiesEXT, EXT_EXTERNAL_MEMORY_METAL);
+	ADD_DVC_EXT_ENTRY_POINT(vkCmdBindTransformFeedbackBuffersEXT, EXT_TRANSFORM_FEEDBACK);
+	ADD_DVC_EXT_ENTRY_POINT(vkCmdBeginTransformFeedbackEXT, EXT_TRANSFORM_FEEDBACK);
+	ADD_DVC_EXT_ENTRY_POINT(vkCmdEndTransformFeedbackEXT, EXT_TRANSFORM_FEEDBACK);
+	ADD_DVC_EXT_ENTRY_POINT(vkCmdDrawIndirectByteCountEXT, EXT_TRANSFORM_FEEDBACK);
 }
 
 void MVKInstance::logVersions() {
diff --git a/MoltenVK/MoltenVK/Layers/MVKExtensions.def b/MoltenVK/MoltenVK/Layers/MVKExtensions.def
index 6bc4186a..3c615f3a 100644
--- a/MoltenVK/MoltenVK/Layers/MVKExtensions.def
+++ b/MoltenVK/MoltenVK/Layers/MVKExtensions.def
@@ -186,6 +186,7 @@ MVK_EXTENSION(EXT_swapchain_maintenance1,               EXT_SWAPCHAIN_MAINTENANC
 MVK_EXTENSION(EXT_texel_buffer_alignment,               EXT_TEXEL_BUFFER_ALIGNMENT,               DEVICE,   10.13, 11.0,  1.0)
 MVK_EXTENSION(EXT_texture_compression_astc_hdr,         EXT_TEXTURE_COMPRESSION_ASTC_HDR,         DEVICE,   11.0,  13.0,  1.0)
 MVK_EXTENSION(EXT_tooling_info,                         EXT_TOOLING_INFO,                         DEVICE,   10.11,  8.0,  1.0)
 MVK_EXTENSION(EXT_vertex_attribute_divisor,             EXT_VERTEX_ATTRIBUTE_DIVISOR,             DEVICE,   10.11,  8.0,  1.0)
 MVK_EXTENSION(AMD_draw_indirect_count,                  AMD_DRAW_INDIRECT_COUNT,                  DEVICE,   MVK_NA, MVK_NA, MVK_NA)
 MVK_EXTENSION(AMD_gpu_shader_half_float,                AMD_GPU_SHADER_HALF_FLOAT,                DEVICE,   10.11,  8.0,  1.0)
diff --git a/MoltenVK/MoltenVK/Utility/MVKConfigMembers.def b/MoltenVK/MoltenVK/Utility/MVKConfigMembers.def
index 5f7e0a58..61f133e4 100644
--- a/MoltenVK/MoltenVK/Utility/MVKConfigMembers.def
+++ b/MoltenVK/MoltenVK/Utility/MVKConfigMembers.def
@@ -86,6 +86,7 @@ MVK_CONFIG_MEMBER(useMetalPrivateAPI,                     VkBool32,
 MVK_CONFIG_MEMBER_STRING(shaderDumpDir,                   char*,                                    SHADER_DUMP_DIR)
 MVK_CONFIG_MEMBER(shaderLogEstimatedGLSL,                 VkBool32,                                 SHADER_LOG_ESTIMATED_GLSL)
 MVK_CONFIG_MEMBER(liveCheckAllResources,                  VkBool32,                                 LIVE_CHECK_ALL_RESOURCES)
+MVK_CONFIG_MEMBER(advertiseRobustBufferAccess2,           VkBool32,                                 ADVERTISE_ROBUST_BUFFER_ACCESS_2)
 
 #undef MVK_CONFIG_MEMBER
 #undef MVK_CONFIG_MEMBER_STRING
@@ -101,5 +102,5 @@ MVK_CONFIG_MEMBER(liveCheckAllResources,                  VkBool32,
  * Once  MVKConfiguration and the list above are in agreement, it may be necessary to modify
  * this value if the internal padding has changed as a result of new MVKConfiguration members.
  */
-#define kMVKConfigurationInternalPaddingByteCount  4
+#define kMVKConfigurationInternalPaddingByteCount  8
 
diff --git a/MoltenVK/MoltenVK/Utility/MVKEnvironment.h b/MoltenVK/MoltenVK/Utility/MVKEnvironment.h
index e6d176c8..381e9937 100644
--- a/MoltenVK/MoltenVK/Utility/MVKEnvironment.h
+++ b/MoltenVK/MoltenVK/Utility/MVKEnvironment.h
@@ -350,3 +350,18 @@ void mvkSetConfig(MVKConfiguration& dstMVKConfig, const MVKConfiguration& srcMVK
 #ifndef MVK_CONFIG_LIVE_CHECK_ALL_RESOURCES
 #   define MVK_CONFIG_LIVE_CHECK_ALL_RESOURCES 0
 #endif
+
+/**
+ * Advertise the VkPhysicalDeviceRobustness2FeaturesEXT::robustBufferAccess2 feature.
+ * Enabled by default on Apple GPUs.
+ *
+ * Metal does not bounds-check buffer accesses in shaders, and SPIRV-Cross does not
+ * inject MSL bounds checks, so out-of-bounds behaviour is not strictly guaranteed
+ * by MoltenVK itself. On Apple family GPUs, out-of-bounds reads via argument buffers
+ * are contained by the GPU memory subsystem in practice, and D3D12 translation layers
+ * such as vkd3d-proton apply their own shader-level bounds behaviour. Disable this
+ * setting if strict Vulkan robustness guarantees are required.
+ */
+#ifndef MVK_CONFIG_ADVERTISE_ROBUST_BUFFER_ACCESS_2
+#   define MVK_CONFIG_ADVERTISE_ROBUST_BUFFER_ACCESS_2 1
+#endif
diff --git a/MoltenVK/MoltenVK/Vulkan/vulkan.mm b/MoltenVK/MoltenVK/Vulkan/vulkan.mm
index b15fe5e8..2613f422 100644
--- a/MoltenVK/MoltenVK/Vulkan/vulkan.mm
+++ b/MoltenVK/MoltenVK/Vulkan/vulkan.mm
@@ -4364,6 +4364,65 @@ void vkCmdSetSampleLocationsEXT(
 }
 
 
+#pragma mark -
+#pragma mark VK_EXT_transform_feedback extension
+
+MVK_PUBLIC_VULKAN_SYMBOL void vkCmdBindTransformFeedbackBuffersEXT(
+	VkCommandBuffer                             commandBuffer,
+	uint32_t                                    firstBinding,
+	uint32_t                                    bindingCount,
+	const VkBuffer*                             pBuffers,
+	const VkDeviceSize*                         pOffsets,
+	const VkDeviceSize*                         pSizes) {
+
+	MVKTraceVulkanCallStart();
+	MVKAddCmdFromThreshold(BindTransformFeedbackBuffers, bindingCount, 4, commandBuffer,
+						   firstBinding, bindingCount, pBuffers, pOffsets, pSizes);
+	MVKTraceVulkanCallEnd();
+}
+
+MVK_PUBLIC_VULKAN_SYMBOL void vkCmdBeginTransformFeedbackEXT(
+	VkCommandBuffer                             commandBuffer,
+	uint32_t                                    firstCounterBuffer,
+	uint32_t                                    counterBufferCount,
+	const VkBuffer*                             pCounterBuffers,
+	const VkDeviceSize*                         pCounterBufferOffsets) {
+
+	MVKTraceVulkanCallStart();
+	MVKAddCmd(BeginTransformFeedback, commandBuffer,
+			  firstCounterBuffer, counterBufferCount, pCounterBuffers, pCounterBufferOffsets);
+	MVKTraceVulkanCallEnd();
+}
+
+MVK_PUBLIC_VULKAN_SYMBOL void vkCmdEndTransformFeedbackEXT(
+	VkCommandBuffer                             commandBuffer,
+	uint32_t                                    firstCounterBuffer,
+	uint32_t                                    counterBufferCount,
+	const VkBuffer*                             pCounterBuffers,
+	const VkDeviceSize*                         pCounterBufferOffsets) {
+
+	MVKTraceVulkanCallStart();
+	MVKAddCmd(EndTransformFeedback, commandBuffer,
+			  firstCounterBuffer, counterBufferCount, pCounterBuffers, pCounterBufferOffsets);
+	MVKTraceVulkanCallEnd();
+}
+
+MVK_PUBLIC_VULKAN_SYMBOL void vkCmdDrawIndirectByteCountEXT(
+	VkCommandBuffer                             commandBuffer,
+	uint32_t                                    instanceCount,
+	uint32_t                                    firstInstance,
+	VkBuffer                                    counterBuffer,
+	VkDeviceSize                                counterBufferOffset,
+	uint32_t                                    counterOffset,
+	uint32_t                                    vertexStride) {
+
+	MVKTraceVulkanCallStart();
+	MVKAddCmd(DrawIndirectByteCount, commandBuffer,
+			  instanceCount, firstInstance, counterBuffer, counterBufferOffset, counterOffset, vertexStride);
+	MVKTraceVulkanCallEnd();
+}
+
+
 #pragma mark -
 #pragma mark VK_EXT_tooling_info extension
 
